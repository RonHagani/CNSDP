/platform
/platform.exe
*.exe

# Local reference-environment secrets (docker-compose.yml, .env.example).
# Never commit real values -- see docs/reference-environment.md.
.env

# Local build/test artifacts.
coverage.out
coverage.html
tmp/

# Generated verification-harness run reports (test/verification) -- local
# evidence artifacts from a run on this machine, never committed; see
# test/verification/report.go.
/test/verification/results/

# Editor and OS metadata.
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local Claude Code configuration (plugin preferences, machine-specific
# permission allowlists, local paths). Not required to build, test, run, or
# contribute to CNSDP -- kept local rather than repository-owned.
.claude/
